#1
bubblewrap init --manifest=https://sourabhsuneja.github.io/jvp-spark/manifest.json

#2
bubblewrap build

#3
keytool -list -v -keystore android.keystore -alias android

#Final: Create assetlogs.json with the SHA-256 key obtained from Step #3 inside a folder .well-known in the root folder of the site

